import * as actionTypes from './actions';

const initialState = {
    salad: 0.5,
    cheese: 0.4,
};

const reduser = (state = initialState, action) => {
    return state;
};

export default reduser;